spring:
  datasource:
    url: jdbc:mysql://localhost:3306/griden_db
    username: griden_user
    password: 4568363
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    org:
      springframework:
        security=DEBUG:

jwt:
  secret: Spasi_HospadiSpasi_HospadiSpasi_HospadiSpasi_Hospadi
